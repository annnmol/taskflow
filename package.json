{
  "name": "taskflow",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "ui",
    "server",
    "api",
    "worker"
  ],
  "scripts": {
    "dev:web": "bun run --filter @taskflow/ui dev",
    "dev:api": "bun run --filter @taskflow/api dev",
    "dev:server": "bun run --filter @taskflow/server dev",
    "dev:worker": "bun run --filter @taskflow/worker dev",
    "dev": "bun run --filter '*' dev",
    "start": "bun run dev",
    "db:up": "docker compose up -d postgres redis minio",
    "db:down": "docker compose down",
    "build": "bun run --filter '*' build"
  }
}
